import { ProjectType, Tree } from '@nx/devkit';
export interface GeneratorSchema {
    directory: string;
    name?: string;
    tags?: string;
    skipFormat?: boolean;
}
export interface GeneratorNormalizedSchema extends GeneratorSchema {
    name: string;
    moduleName: string;
    projectName: string;
    projectRoot: string;
    projectType: ProjectType;
    parsedTags: string[];
}
export declare const normalizeOptions: (tree: Tree, options: GeneratorSchema, projectType: ProjectType) => Promise<GeneratorNormalizedSchema>;
