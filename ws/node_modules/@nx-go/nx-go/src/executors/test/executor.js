"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = runExecutor;
const utils_1 = require("../../utils");
/**
 * This executor tests Go code using the `go test` command.
 *
 * @param options options passed to the executor
 * @param context context passed to the executor
 */
async function runExecutor(options, context) {
    return (0, utils_1.executeCommand)([
        'test',
        ...(0, utils_1.buildFlagIfEnabled)('-v', options.verbose),
        ...(0, utils_1.buildFlagIfEnabled)('-cover', options.cover),
        ...(0, utils_1.buildStringFlagIfValid)(`-coverprofile`, options.coverProfile),
        ...(0, utils_1.buildFlagIfEnabled)('-race', options.race),
        ...(0, utils_1.buildStringFlagIfValid)(`-run`, options.run),
        ...(0, utils_1.buildStringFlagIfValid)('-count', options.count && options.count > 0
            ? options.count.toString()
            : undefined),
        ...(0, utils_1.buildStringFlagIfValid)(`-timeout`, options.timeout),
        ...(0, utils_1.buildStringFlagIfValid)('-tags', options.tags?.join(',')),
        './...',
    ], { cwd: (0, utils_1.resolveWorkingDirectory)(context) });
}
//# sourceMappingURL=executor.js.map