"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = runExecutor;
const utils_1 = require("../../utils");
/**
 * This executor runs a Go program using the `go run` command.
 *
 * @param options options passed to the executor
 * @param context context passed to the executor
 */
async function runExecutor(options, context) {
    const cwd = options.cwd ?? (0, utils_1.resolveWorkingDirectory)(context);
    const mainFile = options.main ?? '.';
    return (0, utils_1.executeCommand)(['run', ...(options.flags ?? []), mainFile, ...(options.args ?? [])], { executable: options.cmd, cwd });
}
//# sourceMappingURL=executor.js.map