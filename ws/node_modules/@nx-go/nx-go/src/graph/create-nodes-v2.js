"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createNodesV2 = void 0;
const devkit_1 = require("@nx/devkit");
const node_fs_1 = require("node:fs");
const node_path_1 = require("node:path");
const constants_1 = require("../constants");
const utils_1 = require("../utils");
const generate_targets_1 = require("./create-nodes/generate-targets");
const has_main_package_1 = require("./create-nodes/has-main-package");
exports.createNodesV2 = [
    `**/${constants_1.GO_MOD_FILE}`,
    async (configFiles, options, context) => (0, devkit_1.createNodesFromFiles)((configFile, options = {}, context) => createNodesInternal(configFile, normalizeOptions(options), context), configFiles, options ?? {}, context),
];
const normalizeOptions = (options) => ({
    buildTargetName: options.buildTargetName ?? 'build',
    serveTargetName: options.serveTargetName ?? 'serve',
    serveAirTargetName: options.serveAirTargetName ?? 'serve:air',
    testTargetName: options.testTargetName ?? 'test',
    lintTargetName: options.lintTargetName ?? 'lint',
    tidyTargetName: options.tidyTargetName ?? 'tidy',
    generateTargetName: options.generateTargetName ?? 'generate',
});
const createNodesInternal = async (configFilePath, options, context) => {
    const projectRoot = (0, node_path_1.dirname)(configFilePath);
    // Skip creating a project if this is the workspace root and it doesn't have a project.json file
    // This prevents unnecessary project creation in single-module repositories
    if (projectRoot === '.' &&
        !(0, node_fs_1.existsSync)((0, node_path_1.join)(context.workspaceRoot, 'project.json'))) {
        return { projects: {} };
    }
    // We assume that the project name is derived from the project root folder name
    // If needed, the user can still rename the project in the configuration file (project.json)
    const projectName = projectRoot === '.' ? undefined : projectRoot.split(/[/\\]/).pop();
    // Check if project is an application (has package main) or a library
    const isApplication = (0, has_main_package_1.hasMainPackage)(context.workspaceRoot, projectRoot, projectName);
    const projectType = isApplication ? 'application' : 'library';
    const detectedTools = {
        air: isApplication &&
            (0, utils_1.shouldCreateAirTarget)(context.workspaceRoot, projectRoot),
        revive: (0, utils_1.shouldUseReviveLinter)(context.workspaceRoot, projectRoot),
    };
    return {
        projects: {
            [projectRoot]: {
                root: projectRoot,
                name: projectName,
                projectType,
                targets: (0, generate_targets_1.generateTargets)(options, isApplication, detectedTools),
            },
        },
    };
};
//# sourceMappingURL=create-nodes-v2.js.map