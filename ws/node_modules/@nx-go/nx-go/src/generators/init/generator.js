"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = initGenerator;
const devkit_1 = require("@nx/devkit");
const constants_1 = require("../../constants");
const utils_1 = require("../../utils");
async function initGenerator(tree, options) {
    if ((0, utils_1.supportsGoWorkspace)()) {
        (0, utils_1.createGoWork)(tree);
    }
    else {
        (0, utils_1.createGoMod)(tree, (0, utils_1.getProjectScope)(tree));
        devkit_1.logger.warn(`Go workspaces are not supported. You need Go >= ${constants_1.GO_WORK_MINIMUM_VERSION}. Fallback to a single Go module.`);
    }
    (0, utils_1.addNxPlugin)(tree);
    if (!options.skipFormat) {
        await (0, devkit_1.formatFiles)(tree);
    }
}
//# sourceMappingURL=generator.js.map