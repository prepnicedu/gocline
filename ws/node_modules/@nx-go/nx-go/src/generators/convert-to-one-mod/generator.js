"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = convertToOneModGenerator;
const devkit_1 = require("@nx/devkit");
const constants_1 = require("../../constants");
const utils_1 = require("../../utils");
async function convertToOneModGenerator(tree, options) {
    if (!tree.exists(constants_1.GO_WORK_FILE)) {
        devkit_1.logger.error('Go workspace not found');
        return;
    }
    const workContent = tree.read(constants_1.GO_WORK_FILE).toString();
    if (/^use /m.test(workContent)) {
        devkit_1.logger.error('Go workspace already in use and cannot be moved to one module');
        return;
    }
    tree.delete(constants_1.GO_WORK_FILE);
    (0, utils_1.createGoMod)(tree, (0, utils_1.getProjectScope)(tree));
    if (!options.skipFormat) {
        await (0, devkit_1.formatFiles)(tree);
    }
}
//# sourceMappingURL=generator.js.map